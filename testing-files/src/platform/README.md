# Platform Detectors

Platform-specific page parsing belongs here.

Recommended future files:

- `codeforces/detector.js`
- `leetcode/detector.js`

Content scripts should eventually call these detectors instead of holding all selectors directly.
