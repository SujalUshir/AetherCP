# Experiments

Use this folder for ideas that are not part of the stable extension yet.

## Rules

- Put current experiments in `experiments/active/`.
- Move failed or paused experiments to `experiments/abandoned/`.
- Do not mix experimental code into `src/` until it has a feature spec.
- Record what was learned, even if the experiment is abandoned.
